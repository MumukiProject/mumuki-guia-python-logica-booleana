---
rules:
- !ruby/hash:ActiveSupport::HashWithIndifferentAccess
  when: !ruby/hash:ActiveSupport::HashWithIndifferentAccess
    these_expectations_failed:
    - se_puede_concentrar Not:UsesIf
  then: no es necesario usar alternativa condicional en `se_puede_concentrar` ya que
    podemos incluir la condición del `if` como parte de lo que retornemos
